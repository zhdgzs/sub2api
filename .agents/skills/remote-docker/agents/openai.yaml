interface:
  display_name: "Remote Docker"
  short_description: "Release Sub2API via GitHub Actions Docker build"
  default_prompt: "Use $remote-docker to prepare a custom release tag and let GitHub Actions build and push the Docker image."
